apiVersion: v1
kind: Service
metadata:
  name: vehicle-registration-system-service
spec:
  type: LoadBalancer
  selector:
    app: vehicle-registration-system
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
